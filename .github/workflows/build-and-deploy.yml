name: Build and Deploy

permissions:
  contents: read

on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string
      pre-release:
        required: false
        default: false
        type: boolean
    secrets:
      USER:
        required: true
      PWD:
        required: true
      DEPLOY_WEBHOOK_URL:
        required: true

env:
  REPO: registry.themcbrothers.net
  NAME: lore-le

jobs:
  quality:
    uses: ./.github/workflows/quality.yml
    with:
      node-version: 24

  build:
    runs-on: ubuntu-latest
    needs: [ quality ]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login to registry
        uses: docker/login-action@v3
        with:
          registry: ${{env.REPO}}
          username: ${{secrets.USER}}
          password: ${{secrets.PWD}}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{env.REPO}}/docker-images/${{env.NAME}}:${{github.ref_name}}
            ${{ format('{0}/docker-images/{1}:{2}', env.REPO, env.NAME, inputs.pre-release && 'beta' || 'latest') }}

  deploy:
    runs-on: ubuntu-latest
    needs: [ build ]
    permissions:
      contents: write
    environment: ${{ inputs.environment }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy stack
        uses: distributhor/workflow-webhook@v3
        with:
          webhook_url: ${{ secrets.DEPLOY_WEBHOOK_URL }}
          silent: true

      - name: GitHub Release
        uses: ncipollo/release-action@v1.20.0
        with:
          allowUpdates: true
          generateReleaseNotes: true
          removeArtifacts: true
          replacesArtifacts: true
          prerelease: ${{ inputs.pre-release }}
